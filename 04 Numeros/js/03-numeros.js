let resultado;

resultado = Math.PI;

// redondeo
resultado = Math.round(2.5);

// Potencia
resultado = Math.pow(8, 3);

resultado = Math.min(3, 5, 1, 2, 9, 4);

resultado = Math.max(3, 5, 1, 2, 9, 4);

console.log(resultado);
